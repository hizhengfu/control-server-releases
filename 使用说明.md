# ControlServer 使用说明

本说明面向安装、配置和日常使用 ControlServer 的现场人员。开发、构建和
发布配置请查阅项目根目录的 `README.md` 和 `docs/public-release.md`。

## 1. 下载

官方安装包只从
[ControlServer Releases](https://github.com/hizhengfu/control-server-releases/releases)
发布。

当前公开发布的 Windows 便携包文件名为：

```text
control-server-windows-x64-base-video-voice.zip
```

请同时下载同一版本下的 `SHA256SUMS`。不要从聊天群、网盘或无法确认来源的
链接获取安装包。

## 2. 校验下载文件

在安装包所在目录打开 PowerShell，执行：

```powershell
Get-FileHash .\control-server-windows-x64-base-video-voice.zip -Algorithm SHA256
Get-Content .\SHA256SUMS
```

第一条命令显示的哈希值必须与 `SHA256SUMS` 中该文件对应的值完全一致。
如果不一致，请删除文件并重新从官方 Releases 下载。

## 3. 安装与启动

Windows 发布包是便携版，不提供安装向导：

1. 把 ZIP 完整解压到一个固定目录，例如 `D:\ControlServer`。
2. 不要直接在 ZIP 压缩包中运行程序。
3. 不要单独移动 EXE；程序目录中的 DLL、模型和其他资源需要保持在一起。
4. 普通使用请双击 `control-server.exe`。
5. 程序启动后会出现在系统托盘。右键托盘图标可打开 Web 管理、系统设置和帮助。

`control-player-node.exe` 是播放节点辅助程序，通常由系统自动使用。除非现场
部署人员明确要求，否则不需要单独启动。

如果 Windows 显示未知发布者、签名异常或安全软件拦截，请先停止运行，确认
下载地址和 SHA-256 校验结果，并联系软件提供方或现场管理员，不要直接忽略
安全提示。

## 4. 首次设置

从系统托盘打开“系统设置”，在“设备与运行拓扑”中选择本机角色：

| 角色 | 适用情况 |
| --- | --- |
| 单机部署 | 只有一台电脑，内容在本机屏幕或投影机播放；一般单机用户优先选此项 |
| 控制与播放一体 | 主控机既在本机播放，又管理其他播放节点 |
| 控制端 | 本机只负责项目、部署和控制，内容由其他播放节点输出 |
| 播放节点 | 本机连接显示屏或投影机，接受控制端下发的播放任务 |

选择后点击“保存设置”。控制与播放一体或单机部署还需要确认默认显示器；
播放节点需要由现场管理员提供节点 ID、控制端地址和 Token。

正式业务控制需要有效产品授权及 `control` 权益。未授权时仍可打开系统设置
和诊断页面，但项目部署、播放控制等业务命令会被拒绝。

## 5. 快速打开本机内容

右键系统托盘图标：

- “打开文件…”：播放图片、文档、视频或 3D 文件。
- “打开文件夹…”：循环播放同一目录中的图片、文档和视频。
- “打开链接…”：打开网页或远程图片、视频、PDF、3D 资源。
- “打开 Web 管理…”：进入完整的项目和播放管理界面。
- “系统设置…”：修改拓扑、显示、网络、安全、授权及高级设置。
- “帮助…”：查看程序内使用帮助。
- “退出”：关闭服务和已打开的内容窗口，升级前应先执行此操作。

默认情况下 Web 管理地址为 `http://127.0.0.1:8080/`。如果端口被占用，程序
可能会使用其他端口，因此优先通过托盘菜单打开。

## 6. 创建并播放正式项目

正式项目建议按以下顺序操作：

1. 从托盘打开“Web 管理”。
2. 进入“项目与场景”，创建或导入项目。
3. 把图片、文档、视频、网页或 3D 文件加入统一素材中心。
4. 为项目绑定本机或远端播放输出。
5. 创建场景并设置各输出需要显示的内容和布局。
6. 有多投影拼接需求时，在“融合校准”中配置并保存校准版本。
7. 部署项目，等待目标节点完成资源准备。
8. 进入“播放控制”，选择场景并执行播放、暂停、恢复或停止。

播放节点不在本机编辑项目。节点上线后会等待控制端下发部署计划，并在
“播放节点状态”中回报连接、资源和输出状态。

## 7. 升级

1. 从托盘选择“退出”，确认程序已经结束。
2. 保留当前可用版本，不要直接覆盖原目录。
3. 下载并校验新版本，把它解压到新的目录。
4. 启动新版本，检查系统设置、授权、节点连接和实际播放。
5. 确认运行正常后，再归档或删除旧版本目录。

升级前不要删除项目资源、配置或缓存目录。它们的实际位置可在“系统设置”
的诊断信息中查看。

## 8. 常见问题

### 启动后没有界面

ControlServer 默认常驻系统托盘，不一定显示主窗口。请在任务栏通知区域展开
隐藏图标，找到程序图标后打开 Web 管理或系统设置。

### Web 管理打不开

先确认托盘图标仍在，再通过托盘菜单打开。若仍失败，请检查本机防火墙、
端口占用和系统设置中的网络诊断信息。

### 内容没有显示在预期屏幕

在“系统设置 → 显示与预览”检查默认显示器；正式项目还需检查项目的输出
绑定和部署目标。

### 播放节点离线

检查播放节点上的控制端地址、节点 ID、Token 和网络连通性，并确认控制端
与播放节点的系统时间正常。

### 视频、语音或手势不可用

这些能力取决于下载的产品包、模型资源、系统设备和权限。请在系统设置中
检查能力状态、模型状态、麦克风/摄像头权限和视频后端。

### 远程预览或控制不可用

屏幕预览需要系统屏幕录制权限；远程鼠标、键盘和滚动还需要辅助功能权限。
修改权限后应完全退出并重新启动程序。

## 9. 获取帮助

反馈问题时请提供：

- 使用的版本号和安装包文件名；
- 当前运行拓扑；
- 操作系统版本；
- 问题发生前的操作步骤；
- 系统设置中与问题相关的状态或诊断信息；
- 必要的截图和日志，但不要发送 Token、授权文件或其他敏感信息。

联系邮箱：`hizhengfu@gmail.com`
